{
  "name": "proyecto",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "npx cap init poke-fire-app com.tuempresa.pokefire",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android",
    "generate-assets": "npx capacitor-assets generate --android --pwa"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "firebase": "^11.8.1"
  }
}
